# === Python ===
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.pytest_cache/

# === Virtuelle Umgebungen ===
# Nie ins Repo — werden lokal mit `bash create_venv.sh` erzeugt.
venv_visitorcounter/
venv_hailo_rpi_examples/
venv_*/
venv/
.venv/
env/
ENV/

# === Laufzeit-Ausgaben des Sensors (werden bei jedem Lauf neu erzeugt) ===
#ergebniss.csv
#zaehlung.csv
#auto_config_points.csv
#bewegungsbild_*.png
#tracked_objects_*.png
#auto_config_clusters.png
#auto_config_border.png

# Referenzbild wird bei jeder Aufnahme neu erzeugt
camera_raw.png

# Archiv vorheriger Läufe (vom Start-Cleanup angelegt)
#vorherige_laeufe/

# Altbestände / manuell abgelegte Track-Dateien
old_track files/
*_altes_format_*.csv

# === Aktive Konfiguration ===
# roi_config.json
!roi_config.example.json wird vom Konfig-Tool erzeugt und ist geräte-/standortspezifisch.
# Nicht ins Repo — jedes Gerät hat seine eigene. Bei Bedarf eine Beispiel-Config
# als roi_config.example.json einchecken.
roi_config.json

# Gemerktes UI-Design (hell/dunkel) — geräte-/nutzerspezifisch, nicht ins Repo.
ui_settings.json

# === Betriebssystem / Editor ===
.DS_Store
*.swp
*~
.idea/
.vscode/

# === Logs / Temp ===
*.log
*.tmp

# === Testvideos (liegen außerhalb des Repos) ===
*.mp4

# Videomitschnitte — duerfen NIE ins Repository
aufnahmen/
*.mkv
*.mp4
*.avi
